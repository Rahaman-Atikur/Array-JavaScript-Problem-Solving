/**Combining Arrays
Instructions:

Create two arrays of your choice.
Use the concat method to combine the two arrays into a new array.
Print both the original arrays and the combined array using console.log(). */
const arrayOfStringOne = ['S','h','u'];

const arrayOfStringTwo = ['v','o'];

const arrayOfStringThree = arrayOfStringOne.concat(arrayOfStringTwo);
console.log(arrayOfStringOne);
console.log(arrayOfStringTwo);
console.log(arrayOfStringThree);